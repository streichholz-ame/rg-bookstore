%main.container
  = render 'carousel'
  
  .container
    .jumbotron.text-center.mb-0
      %h2.h1=t('homepage.welcome')
      .mb-res-50
        %p= t('homepage.about')
      %button.btn.btn-default= t('homepage.get_started')

  = render 'best_sellers'