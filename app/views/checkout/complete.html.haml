%main.container.general-main-wrap
  = render partial: 'checkout_header'
  .text-center.mb-40
    %h3.general-subtitle= t('checkout.complete.thank_you') 
    %p.fw-300= t('checkout.complete.confirmation', email: current_user.email) 
  = simple_form_for(:current_order, url: wizard_path, method: :put) do |f|
    .row.mb-20
      .col-sm-6
        .visible-xs
          %p.general-order-number.mb-0= current_order.number
          %p.general-address= current_order.created_at
        = @orders.receiver_name
        %span.general-edit edit
        %br
        = @orders.delivery_address.address
        %br
        = @orders.full_address
        %br
        = @orders.delivery_address.country
        %br
        = @orders.phone
      .col-sm-6.hidden-xs.text-right
        %p.general-order-number.mb-0= current_order.number
        %p.general-address= current_order.created_at
    .visible-xs
      = render partial: 'order_item_mobile'
    .hidden-xs.divider-lg.pt-20
      %table.table.table-hover
        %thead
          %tr
            %th
              %span.in-grey-600= t('checkout.confirm.book')
            %th.col-method
            %th
              %span.in-grey-600= t('checkout.confirm.price')
            %th
              %span.in-grey-600= t('checkout.confirm.quantity')
            %th
              %span.in-grey-600= t('checkout.confirm.total')
        %tbody
          = render partial: 'order_item'
    .text-center.general-text-right
      %p.in-gold-500.font-18= t('checkout.confirm.summary')
      %table.general-summary-table.general-summary-table-right.general-text-right
        = render partial: 'order_summary'
    .text-center
      = f.submit t('checkout.complete.back_to_store'), class: 'btn btn-default mb-20'
    