%main.container.general-main-wrap
  %h1.mt-0.mb-25= t('order.orders')
  %p.lead.small.mb-10.visible-xs Sort by
  .dropdowns.dropdown.general-order-dropdown
    %a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
      Waiting for processing
      %i.fa.fa-angle-down.dropdown-icon.pull-right
    %ul.dropdown-menu
      %li
        %a{href: "#"} Newest first
      %li
        %a{href: "#"} Popular first
      %li
        %a{href: "#"} Price: Low to hight
      %li
        %a{href: "#"} Price: Low to hight
  .visible-xs
    - @orders_presenter.each do |order|
      = render partial: 'order_item_mobile', locals: { order: order }
  .hidden-xs.mb-res-50
    %table.table.table-hover
      %thead
        %tr
          %th
            %span.in-grey-600= t('order.item_number')
          %th
            %span.in-grey-600= t('order.item_completed_at')
          %th
            %span.in-grey-600= t('order.item_status')
          %th
            %span.in-grey-600= t('order.item_total')
      %tbody
        - current_user.orders.each do |order|
          = render partial: 'order_item', locals: { order: order }
