%a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
  =t('sorting.newest')
  %i.fa.fa-angle-down.dropdown-icon.pull-right
%ul.dropdown-menu
  - SortingService::SORTING_METHODS.each do |key, value|
    %li
      = link_to t("sorting.#{key.to_s}"), params[:id]#, catalog_category_path(sort: key)
      